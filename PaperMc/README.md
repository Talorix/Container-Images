
# Note

**I used this naming format because many people don’t know the PaperMC build number. The latest PaperMC API requires a specific build, and since people are often too lazy to look it up, I went with `paper-{version}.jar` instead of `paper-{version}-{build}.jar`.**
